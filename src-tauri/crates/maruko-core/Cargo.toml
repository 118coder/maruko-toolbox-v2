[package]
name = "maruko-core"
version = "2.0.0"
description = "小丸工具箱V2现代版·纯逻辑核心（无 UI 依赖，可独立测试）"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
libloading = "0.8"
winreg = "0.52"
